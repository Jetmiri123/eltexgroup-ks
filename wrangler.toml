name = "eltexgroup-ks"
main = "worker/index.js"
compatibility_date = "2024-09-01"

[assets]
directory = "."
binding = "ASSETS"
run_worker_first = true

# KV: bind ELTEX_DATA in Cloudflare dashboard → Settings → Bindings
